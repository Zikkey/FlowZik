# FlowZik API - Инструкция для Claude

FlowZik имеет встроенный HTTP API на `localhost:9712` для управления досками, колонками и карточками.

## Включение API

В приложении: **Настройки > Уведомления > API > Включить API-сервер**
Настройка запоминается и автоматически включается при следующем запуске.

## Базовый URL

```
http://localhost:9712
```

---

## ВАЖНО: Используй Node.js вместо curl

curl на Windows ломает кодировку UTF-8 (русский текст превращается в ромбы).
Всегда используй `node -e` для вызовов API:

```javascript
node -e "
const http = require('http');
function api(method, path, body) {
  return new Promise((resolve, reject) => {
    const data = body ? JSON.stringify(body) : null;
    const req = http.request({
      hostname: '127.0.0.1', port: 9712, path, method,
      headers: { 'Content-Type': 'application/json', 'Host': 'localhost:9712' }
    }, res => {
      let buf = '';
      res.on('data', c => buf += c);
      res.on('end', () => { try { resolve(JSON.parse(buf)) } catch { resolve(buf) } });
    });
    req.on('error', reject);
    if (data) req.write(data);
    req.end();
  });
}
(async () => {
  // ... код здесь
})();
"
```

---

## Эндпоинты

### Доски

#### Список досок
```
GET /api/boards
```
Возвращает массив досок с колонками (без карточек, только `cardCount`).

#### Получить доску с карточками
```
GET /api/boards/{boardId}
```
Возвращает доску, колонки и все карточки внутри.

#### Создать доску
```
POST /api/boards
Body: { "title": "Название", "icon": "📋", "columns": [...] }
```
Если `columns` не указаны, создаются 4 стандартные: Бэклог, В работе, Тестирование, Готово.

#### Удалить доску
```
DELETE /api/boards/{boardId}
```
Удаляет доску со всеми колонками и карточками.

---

### Колонки

#### Добавить колонку
```
POST /api/boards/{boardId}/columns
Body: { "title": "Новая колонка", "color": "#6366f1" }
```

---

### Карточки

#### Получить карточку
```
GET /api/cards/{cardId}
```

#### Создать карточку
```
POST /api/boards/{boardId}/cards
Body: {
  "columnId": "{columnId}",
  "title": "Название задачи",
  "description": "Описание задачи",
  "priority": "medium",
  "labels": ["feature"],
  "dueDate": "2026-03-01T00:00:00.000Z",
  "subtasks": [
    {"title": "Подзадача 1"},
    {"title": "Подзадача 2"}
  ]
}
```

**Приоритеты (СТРОГО только эти):** `none`, `low`, `medium`, `high`, `urgent`
**ВАЖНО:** Использование любого другого значения (например `critical`) вызовет краш приложения!

#### Обновить карточку
```
PATCH /api/cards/{cardId}
Body: { "title": "Новое", "priority": "high", "completed": true }
```
Допустимые поля: `title`, `description`, `priority`, `dueDate`, `labels`, `completed`, `subtasks`

#### Удалить карточку
```
DELETE /api/cards/{cardId}
```

#### Переместить карточку в другую колонку
```
POST /api/cards/{cardId}/move
Body: { "columnId": "{targetColumnId}", "index": 0 }
```
`index` - позиция в целевой колонке (необязательно, по умолчанию в конец).

---

### Подзадачи

#### Добавить подзадачу
```
POST /api/cards/{cardId}/subtasks
Body: { "title": "Новая подзадача" }
```

#### Обновить подзадачу
```
PATCH /api/cards/{cardId}/subtasks/{subtaskId}
Body: { "completed": true }
```

#### Удалить подзадачу
```
DELETE /api/cards/{cardId}/subtasks/{subtaskId}
```

---

### Комментарии

#### Добавить комментарий
```
POST /api/cards/{cardId}/comments
Body: { "text": "Текст комментария" }
```

---

## Типичный сценарий работы с Claude

1. Пользователь говорит: "Создай доску для проекта X с задачами"
2. Claude выполняет через `node -e`:
   - `GET /api/boards` — смотрит текущие доски
   - `POST /api/boards` — создает новую доску (или использует существующую)
   - `POST /api/boards/{id}/cards` — создает карточки с приоритетами и подзадачами
3. Данные мгновенно появляются в приложении FlowZik

### Пример: создать доску с задачами

```javascript
node -e "
const http = require('http');
function api(method, path, body) {
  return new Promise((resolve, reject) => {
    const data = body ? JSON.stringify(body) : null;
    const req = http.request({
      hostname: '127.0.0.1', port: 9712, path, method,
      headers: { 'Content-Type': 'application/json', 'Host': 'localhost:9712' }
    }, res => {
      let buf = '';
      res.on('data', c => buf += c);
      res.on('end', () => resolve(JSON.parse(buf)));
    });
    req.on('error', reject);
    if (data) req.write(data);
    req.end();
  });
}
(async () => {
  // Создать доску
  const board = await api('POST', '/api/boards', {
    title: 'Мой проект', icon: '\ud83d\ude80'
  });
  const backlog = board.columns[0].id;

  // Добавить карточки
  await api('POST', '/api/boards/' + board.id + '/cards', {
    columnId: backlog,
    title: 'Первая задача',
    priority: 'medium',
    subtasks: [{ title: 'Шаг 1' }, { title: 'Шаг 2' }]
  });

  console.log('Done');
})();
"
```

## Безопасность

- API слушает **только** на `127.0.0.1` (не доступен извне)
- Проверяет заголовок `Host` — только `localhost` и `127.0.0.1`
- Порт: `9712`
